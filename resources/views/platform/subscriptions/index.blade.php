<x-layouts.platform title="Subscriptions">
    <h1 class="text-3xl font-bold">Subscriptions</h1>
    <form class="mt-6 rounded bg-white p-4 shadow" method="GET"><select class="rounded border p-2" name="status"><option value="">Any status</option>@foreach(['trial','active','past_due','grace','suspended','cancelled','expired'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="rounded bg-slate-900 px-4 py-2 text-white">Filter</button></form>
    <div class="mt-6 rounded bg-white shadow"><table class="w-full text-left text-sm"><thead class="bg-slate-100"><tr><th class="p-3">Workspace</th><th>Plan</th><th>Status</th><th>Trial end</th><th>Period</th><th>Cycle</th><th>Created</th></tr></thead><tbody>@foreach($subscriptions as $subscription)<tr class="border-t"><td class="p-3">{{ $subscription->workspace?->name }}</td><td>{{ $subscription->plan?->name ?? '—' }}</td><td>{{ $subscription->status }}</td><td>{{ $subscription->trial_ends_at?->toDateString() ?? '—' }}</td><td>{{ $subscription->current_period_start?->toDateString() ?? '—' }} / {{ $subscription->current_period_end?->toDateString() ?? '—' }}</td><td>{{ $subscription->billing_cycle ?? '—' }}</td><td>{{ $subscription->created_at?->toDateString() }}</td></tr>@endforeach</tbody></table></div><div class="mt-6">{{ $subscriptions->links() }}</div>
</x-layouts.platform>
