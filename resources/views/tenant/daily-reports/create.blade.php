<x-layouts.app><h1>Daily Report</h1><form method="post" action="{{ $report->exists ? '/daily-reports/'.$report->id : '/daily-reports' }}">@csrf @if($report->exists)@method('PUT')@endif <input type="date" name="report_date" value="{{ old('report_date',$report->report_date?->toDateString() ?? today()->toDateString()) }}"><textarea name="completed_summary" placeholder="Completed">{{ old('completed_summary',$report->completed_summary) }}</textarea><textarea name="in_progress_summary" placeholder="In progress">{{ old('in_progress_summary',$report->in_progress_summary) }}</textarea><textarea name="pending_summary" placeholder="Pending">{{ old('pending_summary',$report->pending_summary) }}</textarea><textarea name="blocker_summary" placeholder="Blockers">{{ old('blocker_summary',$report->blocker_summary) }}</textarea><textarea name="tomorrow_plan" placeholder="Tomorrow plan">{{ old('tomorrow_plan',$report->tomorrow_plan) }}</textarea><button>Save draft</button></form></x-layouts.app>
