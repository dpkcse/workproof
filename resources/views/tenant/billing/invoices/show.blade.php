<x-layouts.app title="Invoice"><h1>{{ $invoice->invoice_number }}</h1><p>{{ $invoice->status }} due {{ $invoice->amount_due }}</p>@foreach($invoice->items as $item)<p>{{ $item->description }} {{ $item->total }}</p>@endforeach<form method="post" action="/billing/manual-payment-request">@csrf<input type="hidden" name="invoice_id" value="{{ $invoice->id }}"><input name="amount" value="{{ $invoice->amount_due }}"><input name="transaction_reference" placeholder="Reference"><button>Submit manual payment</button></form></x-layouts.app>
