<!doctype html><html><head><meta charset="utf-8"><script src="https://cdn.tailwindcss.com"></script><title>Pricing</title></head><body class="bg-slate-50"><main class="mx-auto max-w-6xl p-8"><h1 class="text-4xl font-bold">Simple pricing</h1><p class="mt-3 text-slate-600">Start with a free trial. Payment gateway integration is intentionally deferred.</p><div class="mt-8 grid gap-4 md:grid-cols-4">@forelse($plans as $plan)<div class="rounded bg-white p-6 shadow"><h2 class="text-xl font-semibold">{{ $plan->name }}</h2><p class="mt-2 text-3xl font-bold">{{ $plan->monthly_price > 0 ? $plan->currency.' '.$plan->monthly_price : 'Custom' }}</p><p class="mt-4 text-sm text-slate-600">Users: {{ $plan->user_limit ?? 'Custom' }}</p><p class="text-sm text-slate-600">Projects: {{ $plan->project_limit ?? 'Custom' }}</p><a class="mt-6 inline-block rounded bg-slate-900 px-4 py-2 text-white" href="{{ $plan->slug === 'enterprise' ? '/contact' : '/register' }}">{{ $plan->slug === 'enterprise' ? 'Contact Sales' : 'Start Free Trial' }}</a></div>@empty<p>No active public plans are available yet. Run PlanSeeder.</p>@endforelse</div></main></body></html>
